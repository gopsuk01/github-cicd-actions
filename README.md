create a spring boot file use these commands and push code to git master branch
git init
git status
git add src
git add pom.xml
git commit -m "code"
git branch -M master
git remote add origin https://github.com/gopsuk01/github-cicd-actions.git
git push -u origin master
